/**
 * Merchant About Us Page Loading State
 * Displayed while the merchant about us page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantAboutUsLoading() {
  return <PageLoader />;
}
