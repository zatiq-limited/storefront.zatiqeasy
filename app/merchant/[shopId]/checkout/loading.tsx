/**
 * Merchant Checkout Page Loading State
 * Displayed while the merchant checkout page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantCheckoutLoading() {
  return <PageLoader />;
}
