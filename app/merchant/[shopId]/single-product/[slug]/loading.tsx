/**
 * Merchant Single Product / Landing Page Loading State
 * Displayed while the merchant landing page is loading
 */

import { FullPageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantSingleProductLoading() {
  return <FullPageLoader />;
}
