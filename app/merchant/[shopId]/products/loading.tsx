/**
 * Merchant Products Page Loading State
 * Displayed while the merchant products page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantProductsLoading() {
  return <PageLoader />;
}
