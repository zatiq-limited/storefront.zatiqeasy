/**
 * Merchant Category Details Page Loading State
 * Displayed while the merchant category details page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantCategoryDetailsLoading() {
  return <PageLoader />;
}
