/**
 * Merchant Categories Page Loading State
 * Displayed while the merchant categories page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function MerchantCategoriesLoading() {
  return <PageLoader />;
}
