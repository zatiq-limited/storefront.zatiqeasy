/**
 * Checkout Page Loading State
 * Displayed while the checkout page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function CheckoutLoading() {
  return <PageLoader />;
}
