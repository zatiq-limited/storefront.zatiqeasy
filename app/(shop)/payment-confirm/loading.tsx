/**
 * Payment Confirm Page Loading State
 * Displayed while the payment confirmation page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function PaymentConfirmLoading() {
  return <PageLoader />;
}
