/**
 * Return Policy Page Loading State
 * Displayed while the return policy page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function ReturnPolicyLoading() {
  return <PageLoader />;
}
