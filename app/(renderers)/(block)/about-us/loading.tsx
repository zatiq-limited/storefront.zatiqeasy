/**
 * About Us Page Loading State
 * Displayed while the about us page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function AboutUsLoading() {
  return <PageLoader />;
}
