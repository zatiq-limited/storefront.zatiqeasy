/**
 * Category Details Loading State
 * Displayed while the category details page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function CategoryDetailsLoading() {
  return <PageLoader />;
}
