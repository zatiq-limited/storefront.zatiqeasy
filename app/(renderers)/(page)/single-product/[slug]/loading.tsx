/**
 * Single Product / Landing Page Loading State
 * Displayed while the landing page is loading
 */

import { FullPageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function SingleProductLoading() {
  return <FullPageLoader />;
}
