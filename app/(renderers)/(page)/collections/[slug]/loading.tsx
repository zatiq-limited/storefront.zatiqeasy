/**
 * Collection Details Loading State
 * Displayed while the collection details page is loading
 */

import { PageLoader } from "@/components/shared/skeletons/page-skeletons";

export default function CollectionDetailsLoading() {
  return <PageLoader />;
}
