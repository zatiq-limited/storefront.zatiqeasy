---
import SpecialOffersSlider1 from './SpecialOffersSlider1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<SpecialOffersSlider1 settings={settings} blocks={blocks} pageData={pageData} client:load />
