---
import SpecialOffersSlider2 from './SpecialOffersSlider2.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<SpecialOffersSlider2 settings={settings} blocks={blocks} pageData={pageData} client:load />
