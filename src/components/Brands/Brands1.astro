---
import Brands1 from './Brands1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Brands1 settings={settings} blocks={blocks} pageData={pageData} client:load />
