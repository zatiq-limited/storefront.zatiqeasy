---
import Category5 from './Category5.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Category5 settings={settings} blocks={blocks} pageData={pageData} client:load />
