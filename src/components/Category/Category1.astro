---
import Category1 from './Category1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Category1 settings={settings} blocks={blocks} pageData={pageData} client:load />
