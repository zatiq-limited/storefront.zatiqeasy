---
import Category2 from './Category2.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Category2 settings={settings} blocks={blocks} pageData={pageData} client:load />
