---
import Category6 from './Category6.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Category6 settings={settings} blocks={blocks} pageData={pageData} client:load />
