---
import Badges1 from './Badges1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Badges1 settings={settings} blocks={blocks} pageData={pageData} client:load />
