---
import Badges2 from './Badges2.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Badges2 settings={settings} blocks={blocks} pageData={pageData} client:load />
