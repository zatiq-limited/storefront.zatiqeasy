---
import Badges3 from './Badges3.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<Badges3 settings={settings} blocks={blocks} pageData={pageData} client:load />
