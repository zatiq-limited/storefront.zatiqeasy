---
import ProductCards7 from './ProductCards7.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<ProductCards7 settings={settings} blocks={blocks} pageData={pageData} client:load />
