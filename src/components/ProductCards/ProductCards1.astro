---
import ProductCards1 from './ProductCards1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<ProductCards1 settings={settings} blocks={blocks} pageData={pageData} client:load />
