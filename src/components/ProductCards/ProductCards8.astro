---
import ProductCards8 from './ProductCards8.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<ProductCards8 settings={settings} blocks={blocks} pageData={pageData} client:load />
