---
import ProductCards6 from './ProductCards6.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<ProductCards6 settings={settings} blocks={blocks} pageData={pageData} client:load />
