---
import ProductCards3 from './ProductCards3.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<ProductCards3 settings={settings} blocks={blocks} pageData={pageData} client:load />
