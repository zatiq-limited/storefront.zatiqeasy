---
import PaymentStatus2 from './PaymentStatus2.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<PaymentStatus2 settings={settings} blocks={blocks} pageData={pageData} client:load />
