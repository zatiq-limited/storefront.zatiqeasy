---
import PaymentStatus1 from './PaymentStatus1.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<PaymentStatus1 settings={settings} blocks={blocks} pageData={pageData} client:load />
