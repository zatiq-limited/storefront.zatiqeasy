---
import StaticBanner2 from './StaticBanner2.tsx';

interface Props {
  settings?: Record<string, any>;
  blocks?: any[];
  pageData?: any;
}

const { settings, blocks, pageData } = Astro.props;
---

<StaticBanner2 settings={settings} blocks={blocks} pageData={pageData} client:load />
